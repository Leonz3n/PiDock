# 桌面 Agent 开发工作台

支持跨仓库任务开发及本地验证的桌面工作台。术语用于区分任务、代码隔离、运行环境和 Agent 对话。

## Language

**项目（Project）**：
代表一个业务系统的可复用仓库、普通目录、服务和环境集合。任务按需选择其中的仓库与目录，并可在进行中追加。
_Avoid_: 将项目等同于单个 Git 仓库或一次任务

**任务（Task）**：
一次有明确目标的工作，可以涉及多个代码仓库和普通目录，并与其他任务并行。
_Avoid_: 用项目、仓库或会话指代任务

**任务工作区（Task Workspace）**：
承载一个任务的代码工作副本、普通目录访问入口及本地运行状态的空间。同一仓库参与不同任务时拥有独立工作副本，普通目录的原始文件则可能被多个任务共享。
_Avoid_: 用单个 worktree 指代整个任务工作区，或将任务中的所有文件都视为隔离副本

**环境（Environment）**：
一组有名称的运行配置和远程依赖选择，例如开发环境或测试环境。不同任务可以选择同一个环境，同时拥有各自的本地服务实例。
_Avoid_: 用环境指代整个任务工作区

**共享模板（Shared Template）**：
以项目配置文件保存、可通过版本管理复用的启动方式、环境变量及服务依赖声明。服务继续使用仓库默认配置文件，图形界面维护模板，Agent 修改模板时需展示差异并由用户确认。
_Avoid_: 将某个任务临时生成的运行配置或服务完整配置副本称为共享模板

**仓库默认配置（Repository Default Configuration）**：
由业务仓库维护、随任务代码版本检出的服务配置文件。服务按自身配置规则读取文件，并通过其支持的环境变量调整运行配置。
_Avoid_: 将仓库默认配置与工作台维护的共享模板混为一谈

**本机私有配置（Local Private Configuration）**：
由本机应用单独保存的凭据、本机路径等配置，与共享模板分开管理。
_Avoid_: 将本机私有配置混入可提交的共享模板

**任务覆盖（Task Override）**：
只作用于当前任务的配置调整，例如本地端口和依赖地址。Agent 可在任务范围内自动调整，不因此修改共享模板。
_Avoid_: 将任务覆盖称为环境模板修改

**服务（Service）**：
可以独立启动并参与功能验证的运行单元。服务与代码仓库不要求一一对应。
_Avoid_: 用仓库代替服务

**本地验证（Local Verification）**：
针对任务中修改后的本地代码，通过启动服务、查看日志、执行测试或操作浏览器验证功能的过程。远程依赖可以参与验证，修改后的服务无需先部署到共享环境。
_Avoid_: 将本地验证等同于单元测试或断点调试

**Agent 会话（Agent Session）**：
Agent 围绕任务开展的一段对话及其执行上下文。
_Avoid_: 用会话指代任务或任务工作区

**定时任务（Scheduled Task）**：
带有一次性时间或周期规则的任务类型，拥有固定任务工作区，并按规则创建 Agent 会话。普通任务由用户手动开始工作，定时任务由时间触发工作。
_Avoid_: 将定时任务建模为脱离任务的项目级规则，或与业务项目中的调度器混淆

**定时执行（Scheduled Run）**：
定时任务的一次触发结果，关联该次新建的 Agent 会话；会话不继承其他执行的对话上下文，但使用同一个任务工作区，且历史可查看并继续对话。
_Avoid_: 将多次触发合并为同一个长期会话，或为每次执行重复创建任务工作区

**Provider 配置（Provider Profile）**：
用户在桌面应用中保存的一组模型服务连接信息，包括显示名称、服务地址、认证引用和可用模型。同一供应商可以有多个独立配置，会话选择其中一个配置及其模型。
_Avoid_: 将 Provider 配置等同于模型名称或供应商名称

**上下文占用（Context Usage）**：
当前会话用于模型请求的上下文大小及其相对当前模型窗口的占比，可以包含估算或暂不可用的状态。
_Avoid_: 将上下文占用等同于累计 Token 消耗

**Token 用量（Token Usage）**：
模型调用产生的输入、输出及供应商报告的缓存等 Token 消耗，归属于实际执行时的会话和 Provider 配置。上下文压缩或切换模型不清零已有消耗。
_Avoid_: 将未知用量当作零消耗，或将重复展示历史消息当作新消耗

**上下文引用（Context Reference）**：
用户在对话输入中明确选择的文件、目录或代码片段，带有所属任务、仓库和来源信息。引用的显示名称与实际来源分开记录，发送时可查看其提供给 Agent 的内容范围。
_Avoid_: 将含有 @ 的普通文字自动当作已解析的引用

**技能调用（Skill Invocation）**：
用户从可用技能中选择一个具体来源并让 Agent 按其指引处理当前请求，可附带参数。快捷语法不改变技能来源、适用范围或工具权限。
_Avoid_: 将 $技能名 解释为 shell 变量展开或脚本执行

**斜杠命令（Slash Command）**：
通过对话输入框选择的应用操作、提示模板或受支持扩展入口，其类型和执行结果在界面中明确呈现。
_Avoid_: 将所有以 / 开头的文字当作可直接交给模型或终端执行的命令

**远程访问入口（Remote Access Endpoint）**：
供已授权远程设备访问某一台 PiDock 主机的网络入口。入口可以来自私有网络或自建中转服务，但都使用同一套设备授权与 PiDock 领域权限。
_Avoid_: 将远程访问入口称为 Pi 服务器，或认为入口可直接暴露 Pi RPC 和本机端口

**PiDock Gateway**：
由用户自行部署、在远程设备与 PiDock 主机之间转发受控请求和事件的服务。PiDock 主机主动连接 Gateway，远程设备不直接连接本机监听端口。
_Avoid_: 将 Gateway 与业务环境的远程依赖、VPN 节点或 Pi 运行时混为一谈

**远程设备（Remote Device）**：
经用户配对并获得独立权限的浏览器或终端。每台设备拥有可单独查看、调整和撤销的身份，不与用户账号或主机身份互换。
_Avoid_: 多台手机共享一个不可区分、不可单独撤销的长期令牌

**配对凭据（Pairing Credential）**：
将远程设备绑定到指定 PiDock 主机的一次性短时证明。使用、过期或用户刷新后失效，成功配对后换取独立的设备凭据。
_Avoid_: 将长期设备令牌、项目名称、本机路径或其他敏感状态编码进二维码

**任务浏览器视图（Task Browser View）**：
属于某个任务、用户可见且可接管的真实页面，Agent 与用户操作同一页面实例。同任务的标签页与登录弹窗共享该任务的浏览器登录状态，不同任务的登录状态互不共享；任务页面与 PiDock 自有界面分别归属不同的信任范围。
_Avoid_: 将其称为内嵌 iframe、与品牌界面合并为一个视图，或用另一个隐藏浏览器的结果代表它的状态

**PiDock Host**：
承载任务调度、受管执行与本机资源管理的本机能力层，与界面渲染层分离，并管理各任务工作区的 Agent Host。界面通过受控接口请求 Host 能力，远程访问入口不放宽其本机权限。
_Avoid_: 将 PiDock Host 称为后端服务、理解为需要额外部署的云服务，或与单个任务工作区的 Agent Host 混为一谈

**Agent Host**：
归属于一个任务工作区、承载该工作区 Agent 会话与执行的独立运行单元，通过受控接口请求任务浏览器等本机能力。一个任务工作区可以包含多个仓库，工作区内多个会话仍遵循统一执行协调。
_Avoid_: 将 Agent Host 等同于一个 Git 仓库、界面标签或全局调度器
